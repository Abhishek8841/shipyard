{
  "name": "shipyard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate:db" : "npm run generate -w @shipyard/database",
    "build:packages": "npm run generate:db && npm run build -w @shipyard/database && npm run build -w @shipyard/redis && npm run build -w @shipyard/shared && npm run build -w @shipyard/metrics",
    "build:apps": "npm run build -w @shipyard/api && npm run build -w @shipyard/builder && npm run build -w @shipyard/proxy",
    "build": "npm run build:packages && npm run build:apps"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@shipyard/builder": "^1.0.0",
    "@shipyard/frontend": "^1.0.0",
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
